import React from 'react';
import ReactDOM from 'react-dom/client';
import './migrateBook';
import App from './RecoveryEnhancements';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
